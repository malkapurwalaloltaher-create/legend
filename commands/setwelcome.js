const { SlashCommandBuilder, ChannelType }=require("discord.js");const { requireStaffInteraction, requireStaffMessage, getOrCreateConfig, makeBasicEmbed }=require("../utils/moderationHelpers");module.exports={data:new SlashCommandBuilder().setName("setwelcome").setDescription("Set welcome channel").addChannelOption(o=>o.setName("channel").setDescription("Welcome channel").addChannelTypes(ChannelType.GuildText).setRequired(true)),async execute(i){if(!requireStaffInteraction(i))return;const ch=i.options.getChannel("channel");const cfg=await getOrCreateConfig(i.guildId);cfg.welcomeChannelId=ch.id;cfg.welcomeChannelName=ch.name;await cfg.save();return i.reply({embeds:[makeBasicEmbed("✅ Welcome Channel Saved",`Welcome channel saved as ${ch}`,0x22c55e)]});},async executePrefix(m){if(!requireStaffMessage(m))return;const ch=m.mentions.channels.first();if(!ch)return m.reply("❌ Mention a channel.");const cfg=await getOrCreateConfig(m.guildId);cfg.welcomeChannelId=ch.id;cfg.welcomeChannelName=ch.name;await cfg.save();return m.reply({embeds:[makeBasicEmbed("✅ Welcome Channel Saved",`Welcome channel saved as ${ch}`,0x22c55e)]});}};

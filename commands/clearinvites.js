const { SlashCommandBuilder }=require("discord.js");const { requireStaffInteraction, requireStaffMessage, createCase, makeBasicEmbed }=require("../utils/moderationHelpers");async function clear(guild){const invites=await guild.invites.fetch();let count=0;for(const inv of invites.values()){await inv.delete("Clear invites command").catch(()=>null);count++;}return count;}module.exports={data:new SlashCommandBuilder().setName("clearinvites").setDescription("Delete all server invites"),async execute(i){if(!requireStaffInteraction(i))return;const count=await clear(i.guild);const c=await createCase(i.guild,{action:"CLEARINVITES",targetId:i.guild.id,targetTag:i.guild.name,moderatorId:i.user.id,moderatorTag:i.user.tag,reason:`Deleted ${count} invites`});return i.reply({embeds:[makeBasicEmbed("🔗 Invites Cleared",`Deleted **${count}** invites. Case #${c.caseId}`,0xef4444)]});},async executePrefix(m){if(!requireStaffMessage(m))return;const count=await clear(m.guild);const c=await createCase(m.guild,{action:"CLEARINVITES",targetId:m.guild.id,targetTag:m.guild.name,moderatorId:m.author.id,moderatorTag:m.author.tag,reason:`Deleted ${count} invites`});return m.reply({embeds:[makeBasicEmbed("🔗 Invites Cleared",`Deleted **${count}** invites. Case #${c.caseId}`,0xef4444)]});}};

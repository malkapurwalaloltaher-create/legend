const { SlashCommandBuilder }=require("discord.js");const { requireStaffInteraction, requireStaffMessage, createCase, makeBasicEmbed }=require("../utils/moderationHelpers");async function make(channel){return channel.createInvite({maxAge:86400,maxUses:0,reason:"Dashboard/police invite command"});}module.exports={data:new SlashCommandBuilder().setName("createinvite").setDescription("Create an invite for this channel"),async execute(i){if(!requireStaffInteraction(i))return;const inv=await make(i.channel);const c=await createCase(i.guild,{action:"CREATEINVITE",targetId:i.channel.id,targetTag:`#${i.channel.name}`,moderatorId:i.user.id,moderatorTag:i.user.tag,reason:`Invite ${inv.code} created`});return i.reply({embeds:[makeBasicEmbed("🔗 Invite Created",`${inv.url}\nCase #${c.caseId}`,0x22c55e)],ephemeral:true});},async executePrefix(m){if(!requireStaffMessage(m))return;const inv=await make(m.channel);const c=await createCase(m.guild,{action:"CREATEINVITE",targetId:m.channel.id,targetTag:`#${m.channel.name}`,moderatorId:m.author.id,moderatorTag:m.author.tag,reason:`Invite ${inv.code} created`});return m.reply({embeds:[makeBasicEmbed("🔗 Invite Created",`${inv.url}\nCase #${c.caseId}`,0x22c55e)]});}};

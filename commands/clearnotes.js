const { SlashCommandBuilder } = require("discord.js");const { requireStaffInteraction, requireStaffMessage, ModerationNote, createCase, makeBasicEmbed } = require("../utils/moderationHelpers");module.exports={data:new SlashCommandBuilder().setName("clearnotes").setDescription("Clear member notes").addUserOption(o=>o.setName("user").setDescription("Member").setRequired(true)),async execute(i){if(!requireStaffInteraction(i))return;const u=i.options.getUser("user");const r=await ModerationNote.deleteMany({guildId:i.guildId,userId:u.id});const c=await createCase(i.guild,{action:"CLEARNOTES",targetId:u.id,targetTag:u.tag,moderatorId:i.user.id,moderatorTag:i.user.tag,reason:`Cleared ${r.deletedCount} notes`});return i.reply({embeds:[makeBasicEmbed("✅ Notes Cleared",`Cleared **${r.deletedCount}** notes for ${u}. Case #${c.caseId}`,0x22c55e)]});},async executePrefix(m){if(!requireStaffMessage(m))return;const u=m.mentions.users.first();if(!u)return m.reply("❌ Mention a user.");const r=await ModerationNote.deleteMany({guildId:m.guildId,userId:u.id});const c=await createCase(m.guild,{action:"CLEARNOTES",targetId:u.id,targetTag:u.tag,moderatorId:m.author.id,moderatorTag:m.author.tag,reason:`Cleared ${r.deletedCount} notes`});return m.reply({embeds:[makeBasicEmbed("✅ Notes Cleared",`Cleared **${r.deletedCount}** notes for ${u}. Case #${c.caseId}`,0x22c55e)]});}};
